import 'package:flutter/material.dart';

class ErrandsMovingPage extends StatelessWidget {
  const ErrandsMovingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Errands & Moving Page'),
      ),
      body: Center(
        child: Text('Welcome to My New Page!'),
      ),
    );
  }
}
