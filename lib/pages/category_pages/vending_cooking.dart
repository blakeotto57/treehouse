import 'package:flutter/material.dart';

class VendingCookingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Vending & Cooking Page'),
      ),
      body: Center(
        child: Text('Welcome to My New Page!'),
      ),
    );
  }
}
